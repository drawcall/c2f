export default{
    rule(key){
        switch (key) {
            case "background-color":
                
                break;
        
            default:
                break;
        }
    }
}